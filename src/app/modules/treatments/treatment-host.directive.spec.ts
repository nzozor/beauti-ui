import { TreatmentHostDirective } from './treatment-host.directive';

describe('TreatmentHostDirective', () => {
  it('should create an instance', () => {
    const directive = new TreatmentHostDirective();
    expect(directive).toBeTruthy();
  });
});
